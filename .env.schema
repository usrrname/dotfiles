# This env file uses @env-spec - see https://varlock.dev/env-spec for more info
# @plugin(@varlock/1password-plugin)
# # @initOp(allowAppAuth=true, account=Private) # init via custom root decorator# 1. Load the plugin
# ----------

# @sensitive
GEMINI_API_KEY_1=exec('op read "op://Private/gemini API key/api-key"')
#@sensitive @optional
GEMINI_API_KEY_2=exec('op read "op://Private/gemini API key 2/api-key"')

# @sensitive
OPENCODE_API_KEY=exec('op read "op://Private/opencode/API key"')
# @string
TERM = xterm-256color

