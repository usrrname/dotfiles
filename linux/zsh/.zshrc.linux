# Linux specific zshrc

# Load fnm (Fast Node Manager)
eval "$(fnm env --use-on-cd)"

# uv
eval "$(uv generate-shell-completion zsh)"

# nvim
export PATH="/opt/nvim/bin:$PATH"

# Linux aliases (if any)
if [ -f "$HOME/.aliases" ]; then
  source "$HOME/.aliases"
fi
